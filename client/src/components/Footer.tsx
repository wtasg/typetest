import { Component } from 'solid-js';

const Footer: Component = () => (
    <footer class="footer">
        Typing Test MVP — offline-first, runs in the browser.
    </footer>
);

export default Footer;
