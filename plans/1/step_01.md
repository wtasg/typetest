# Step 01 — Project scaffold

Goal: Create a minimal repository scaffold and toolchain for client and server.

Tasks:

- Initialize repo structure (`client/`, `server/`, `plans/`, `docs/`).
- Add minimal project README and LICENSE placeholder.
- Create `client` with Vite + SolidJS TypeScript starter.
- Create `server` with a Go module and `cmd/server/main.go` starter.
- Add basic `.gitignore` and development scripts.

Deliverable: runnable dev starts for client and server.
