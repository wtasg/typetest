# Step 02 — Typing engine core types

Goal: Define TypeScript types and data model for runs, events, sources, and configuration.

Tasks:

- Draft `Run`, `Event`, `Source`, `Configuration` types.
- Specify normalization and whitespace models (`SPACE`, `TAB`, `NEWLINE`).
- Add example JSON schemas and a types file in `client/src/typing/types.ts`.

Deliverable: `types.ts` and JSON examples for tests.
