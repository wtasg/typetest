# Step 08 — Metrics and reports

Goal: Compute raw and effective metrics and provide report views.

Tasks:

- Implement `metrics.ts` to compute Effective WPM, Raw WPM, accuracy, distributions.
- Create report components for single-run and aggregated views (7-day, 30-day, all-time).
- Add charts (small, minimal) for key distributions and timing histograms.

Deliverable: `client/src/typing/metrics.ts` and `client/src/components/reports`.
