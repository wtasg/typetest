# Step 09 — Game types and configuration

Goal: Implement configuration-driven game types and expose settings UI.

Tasks:

- Define `games/types.ts` and sample implementations for Normal, Sudden Death, Racing, No Correction.
- Add settings UI to configure default game type, time limits, tab width, visible whitespace.
- Ensure run objects store the configuration used.

Deliverable: `client/src/games` and settings persistence.
