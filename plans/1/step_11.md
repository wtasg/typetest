# Step 11 — Replay and racing

Goal: Implement replay of completed runs and racing against previous runs.

Tasks:

- Build a replay renderer that consumes `events` and `startedAt` + `t` timestamps.
- Implement a racing overlay that compares effective progress over time.
- Add UI to select a previous run as the opponent.

Deliverable: replay UI and racing comparison.
